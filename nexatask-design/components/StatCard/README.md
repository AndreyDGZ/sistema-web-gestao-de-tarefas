KPI display used at the top of Home and every service section (Hotels, Adventures, Rent a Car…). Two variants: the dark **hero panel** (`surface-sunken`, `radius-xl`) for the single top-of-page "Overall Performance" summary, holding 3–5 tiles; and the light **card** (`surface-card`, `radius-lg`, `shadow-sm`) repeated per metric elsewhere on the page. Provide a value (`stat-value` style, right numeral formatted with thousands separators and a trailing currency mark where relevant) and a one- or two-word label below it in `caption`. Give the hero panel a leading `brand-accent` icon chip and its section name; individual cards take no icon by default so the number stays the loudest thing in the tile. Never mix more than one accent colour among the cards in a single row.
