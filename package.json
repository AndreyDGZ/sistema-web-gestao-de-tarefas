{
  "name": "nexatask",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": "22.x"
  },
  "workspaces": [
    "apps/web",
    "apps/api",
    "packages/shared"
  ],
  "scripts": {
    "dev": "npx vercel dev",
    "build": "npm run build -w @nexatask/shared && npm run typecheck -w @nexatask/api && npm run build -w @nexatask/web",
    "typecheck": "npm run typecheck -w @nexatask/shared && npm run typecheck -w @nexatask/api && npm run typecheck -w @nexatask/web",
    "preview": "npm run preview -w @nexatask/web"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
